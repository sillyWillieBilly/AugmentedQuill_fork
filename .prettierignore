# Keep the prettier file scope identical to the pre-commit prettier hook's
# `exclude` regex and to the GitHub CI prettier check.
node_modules
dist
coverage
.local-data
venv
.venv
src/frontend/tests/e2e/.test-artifacts
